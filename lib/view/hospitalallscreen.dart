import 'package:flutter/material.dart';

class Hospitalallscreen extends StatelessWidget {
  const Hospitalallscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Column(children: [],),);
  }
}