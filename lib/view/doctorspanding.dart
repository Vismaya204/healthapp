import 'package:flutter/material.dart';

class Doctorspanding extends StatelessWidget {
  const Doctorspanding({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}